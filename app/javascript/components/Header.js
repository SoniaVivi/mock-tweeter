import React from "react";
import PropTypes from "prop-types";

const Header = (props) => {
  return (
    <React.Fragment>
      <button></button>
      <button></button>
    </React.Fragment>
  );
};

export default Header;
